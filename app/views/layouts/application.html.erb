<!DOCTYPE html>
<html>
<head>
  <title>Spizdilooki</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div class="topbar">
	      <div class="fill">
	        <div class="container">
          
			<%= link_to "Spizdilooki", root_path, :class => :brand %>
	          <ul class="nav">
	            <li class = <%= is_home? %> ><%= link_to 'Home', root_path %></li>
	            <li class = <%= is_active?("about") %> ><%= link_to 'About', static_path("about") %></li>
	            <li class = <%= is_active?("contact") %> ><%= link_to 'Contact', static_path("contact") %></li>
				
	          </ul>
	        </div>
	      </div>
	</div>
	
	
	<% unless flash[:success].blank? %>
	<div class="container low alert-message success fade in" data-alert="alert">
	            <a class="close" href="#">×</a>
	            <p><%= flash[:success] %></p>
	</div>
	<% end %>
	<% unless flash[:error].blank? %>
	<div class="container low alert-message error fade in" data-alert="alert">
	            <a class="close" href="#">×</a>
	            <p><%= flash[:error] %></p>
	</div>
	<% end %>
	
	
  	<div class="container low">

		<%= yield %>
	
  	</div>
</body>
</html>
